import { InfinityListDirective } from './infinity-list.directive';

describe('InfinityListDirective', () => {
  it('should create an instance', () => {
    const directive = new InfinityListDirective();
    expect(directive).toBeTruthy();
  });
});
